public interface Forma {
    // Only can have ABSTRACT ATTRIBUTES
    final static int quantidade = 0; // final indicates that the value is constant and cannot be changed

    // Abstract methods
    public float calculaArea();
    public float calculaPerimetro(); 
}